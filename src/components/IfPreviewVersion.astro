---
import { getDefaultVersion, resolveVersion } from '../composables/version'
import type { Version } from '../types/version.ts'

interface Props {
    version?: Version
}

const { version = getDefaultVersion(Astro.url) } = Astro.props
const currentVersion = resolveVersion(version, 'current')
const previewVersion = resolveVersion(version, 'preview')
---

{currentVersion && previewVersion && currentVersion !== previewVersion && <slot />}
