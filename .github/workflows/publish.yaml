name: Publish

on:
  - workflow_dispatch

jobs:

  publish:

    name: Publish Website
    runs-on: ubuntu-24.04

    steps:

      - name: Checkout Website Repository
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Resolve tinylog Version
        run: |
          pip3 install yq
          echo "TINYLOG_VERSION=$(yq -r '.params.stableVersion' config.yaml)" >> $GITHUB_ENV

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Java
        uses: actions/setup-java@v4
        with:
          java-version: 8
          distribution: zulu

      - name: Install Dependencies
        run: npm install

      - name: Build Website
        run: npm run build:prod

      - name: Checkout tinylog Repository
        uses: actions/checkout@v4
        with:
          repository: tinylog-org/tinylog
          ref: ${{ env.TINYLOG_VERSION }}
          path: tinylog

      - name: Generate API documentation
        run: |
          cd tinylog/tinylog-api
          mvn -P release --no-transfer-progress --batch-mode javadoc:javadoc

      - name: Copy API documentation
        run: |
          rm -r public/javadoc/*
          cp -R tinylog/tinylog-api/target/site/apidocs/* public/javadoc

      - name: Upload Assets
        uses: kevinpainchaud/simple-ftp-deploy-action@v1.2.1
        with:
          ftp_host: ${{ vars.TINYLOG_WEBSITE_FTP_PASSWORD }}
          ftp_username: ${{ vars.TINYLOG_WEBSITE_FTP_PASSWORD }}
          ftp_password: ${{ secrets.TINYLOG_WEBSITE_FTP_PASSWORD }}
          local_source_dir: public/assets/
          dist_target_dir: assets/

      - name: Sync Website
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ vars.TINYLOG_WEBSITE_FTP_PASSWORD }}
          username: ${{ vars.TINYLOG_WEBSITE_FTP_PASSWORD }}
          password: ${{ secrets.TINYLOG_WEBSITE_FTP_PASSWORD }}
          protocol: ftps
          local-dir: public/
          exclude: |
            assets/*
            matomo/*
            p2-repository/*
